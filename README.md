# hyperoot

A CLI persona app that presents my work in the terminal.

## Releases

This project uses Commitizen for automated versioning and GitHub Actions to publish releases to PyPI. See `RELEASING.md` for details.

## Contributing

- Use Conventional Commits for commit messages.
- Install and run pre-commit: `python -m pip install pre-commit && pre-commit install`.
